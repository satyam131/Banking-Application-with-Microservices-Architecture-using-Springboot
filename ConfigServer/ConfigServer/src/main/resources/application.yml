server:
  port: 9003

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/satyam131/microservice-configuration
          clone-on-start: true
